{{ form_start(form) }}
    {{ form_widget(form, {"attr":{'data-turbo':'false'}}) }}<br>
    <button class="btn btn-outline-primary">{{ button_label|default('Save') }}</button><br>
{{ form_end(form) }}
